{
    "name": "Sinergym Development",
    "dockerComposeFile": "../docker-compose.yml",
    "service": "sinergym",
    "workspaceFolder": "/workspace",
    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": []
        }
    },
    "forwardPorts": [],
    "postCreateCommand": "pip install -r requirements.txt"
}